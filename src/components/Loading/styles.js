import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
  fullBox: {
    minHeight: "90vh"
  },
  standardBox: {
    minHeight: "100%"
  },
  progress: {
    margin: theme.spacing(1)
  }
}));
